/**
 * SecuritySettings — settings hub page for authentication and SSO.
 * The actual SSO configuration UI is provided by the existing SSOSettings component.
 */
import SSOSettings from "@/pages/admin/SSOSettings";

export default function SecuritySettings() {
  return <SSOSettings />;
}
